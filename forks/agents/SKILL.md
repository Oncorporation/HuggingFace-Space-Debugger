---
name: huggingface-space-debugger
description: Diagnose failing Hugging Face Spaces. Use for Space startup, build, Gradio 6, ZeroGPU, and hardware failures.
license: MIT
metadata:
  version: "2.1.0"
  fork: agents
---

# Agents-store fork

Canonical install path for Copilot / Codex / Cline / Amp / APM-Hermes project scope.

- Project: `.agents/skills/huggingface-space-debugger/`
- User: `~/.agents/skills/huggingface-space-debugger/`

Copy the entire parent skill (scripts + references + templates). This file is a pointer, not a standalone skill.

Keep `.agents` and `.copilot` forks both installed when the user uses both harnesses.
